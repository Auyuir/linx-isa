# C++ Models (planned)

This directory will contain C++ reference models (functional model, performance model, etc.).

The decode/encode tables should be generated from `isa/spec/current/linxisa-v0.1.json`.
